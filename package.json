{
	"devDependencies": {
		"globby": "^6.1.0",
		"lerna": "^2.1.0"
	},
	"scripts": {
		"banners": "util/maintain-banners.js",
		"publish": "lerna publish --skip-npm",
		"bootstrap": "lerna bootstrap --hoist",
		"install:packages": "lerna run install:package",
		"test": "lerna run test"
	}
}
