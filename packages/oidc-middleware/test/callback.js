describe('/authorize/callback', () => {

  it('should exchange code for tokens and save the userinfo and tokens in the session', () => {

  });

  it('should render errors as an html response', () => {

  });

});