
describe('oidc middleware', () => {

  it('should attach login and callback handlers to the express app', () => {

  });

  it('should allow me to change the path of the login and callback handlers', () => {

  });

  it('should allow me to provide my own login and callback handlers', () => {

  });

  it('should allow me to change the session key', () => {

  });

  it('populate req.userInfo and req.tokens if user has authenticated', () => {

  });

});


describe('oidc.ensureLoggedIn()', () => {

  it('should redirect to the login route if no session and request prefers text/html', () => {

  });

  it('should 401 if no session and request prefers application/json', () => {

  });

});

